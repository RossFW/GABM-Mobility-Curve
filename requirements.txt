# Install into a fresh venv:
#   python3 -m venv venv && source venv/bin/activate
#   pip install -r requirements.txt
#
# The LLM provider layer is vendored in providers/ — this repo is
# self-contained (see docs/SETUP.md). API keys go in a root .env file.

anthropic>=0.49.0
openai>=1.68.0
google-genai>=1.0.0
python-dotenv>=1.0.0
